# Port (Railway sets PORT automatically, 3000 is default fallback)
PORT=3000

# Hugging Face User Access Token with Read permission (Required for inference)
HF_TOKEN=hf_your_huggingface_read_token_here

# Environment
NODE_ENV=production
